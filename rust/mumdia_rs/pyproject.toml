[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "mumdia_rs"
version = "0.1.0"
description = "Rust-accelerated numerical functions for MuMDIA proteomics pipeline"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
